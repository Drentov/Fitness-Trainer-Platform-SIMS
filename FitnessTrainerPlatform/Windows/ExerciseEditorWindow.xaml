<Window x:Class="FitnessTrainerPlatform.Windows.ExerciseEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add Exercise"
        Height="420" Width="440"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource PageBackground}">
    <Border Background="White" Margin="16" Padding="20" CornerRadius="8">
        <StackPanel>
            <TextBlock Text="Exercise name" Foreground="#65676B"/>
            <TextBox x:Name="NameBox"/>

            <TextBlock Text="Short description" Foreground="#65676B"/>
            <TextBox x:Name="DescBox" AcceptsReturn="True" Height="60" TextWrapping="Wrap"/>

            <TextBlock Text="Duration (e.g. 20 push-ups, 15 min swimming)" Foreground="#65676B"/>
            <TextBox x:Name="DurationBox"/>

            <TextBlock Text="Video file path (trainer recording)" Foreground="#65676B"/>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox x:Name="VideoBox" Margin="0,4,8,12"/>
                <Button Grid.Column="1" Content="Browse..." Style="{StaticResource SecondaryButton}" Click="Browse_Click"/>
            </Grid>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Cancel" Style="{StaticResource SecondaryButton}" Click="Cancel_Click" Margin="0,0,8,0"/>
                <Button Content="Save" Click="Save_Click"/>
            </StackPanel>
        </StackPanel>
    </Border>
</Window>
