<Window x:Class="FitnessTrainerPlatform.Windows.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="FitTrain Platform - Login"
        Height="500" Width="420"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="{StaticResource PageBackground}">
    <Border Background="White" CornerRadius="12" Margin="24" Padding="32">
        <StackPanel VerticalAlignment="Center">
            <TextBlock Text="FitTrain" FontSize="32" FontWeight="Bold" Foreground="{StaticResource PrimaryBrush}" HorizontalAlignment="Center"/>
            <TextBlock Text="Fitness Trainer Platform" FontSize="14" Foreground="#65676B" HorizontalAlignment="Center" Margin="0,0,0,24"/>

            <TextBlock Text="Username" Foreground="#65676B"/>
            <TextBox x:Name="UsernameBox"/>

            <TextBlock Text="Password" Foreground="#65676B"/>
            <PasswordBox x:Name="PasswordBox" KeyDown="PasswordBox_KeyDown"/>

            <Button Content="Log In" Click="Login_Click" Margin="0,8,0,8"/>
            <Button Content="Register" Style="{StaticResource SecondaryButton}" Click="Register_Click"/>

            <TextBlock Margin="0,20,0,0" TextWrapping="Wrap" Foreground="#65676B" FontSize="11" TextAlignment="Center"
                       Text="Demo accounts: admin/admin123, john/user123, coach_mike/trainer123"/>
        </StackPanel>
    </Border>
</Window>
