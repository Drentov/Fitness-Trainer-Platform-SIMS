<Window x:Class="FitnessTrainerPlatform.Windows.ChatWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Internal Chat"
        Height="560" Width="420"
        MinHeight="400" MinWidth="360"
        WindowStartupLocation="CenterScreen"
        Background="#FFFFFF">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#0084FF" Padding="16,12">
            <StackPanel>
                <TextBlock x:Name="ChatTitle" Foreground="White" FontWeight="Bold" FontSize="16"/>
                <TextBlock Text="Inter-process chat (open 2 app instances to demo)" Foreground="#DDEEFF" FontSize="11"/>
            </StackPanel>
        </Border>

        <ScrollViewer x:Name="MessagesScroll" Grid.Row="1" VerticalScrollBarVisibility="Auto" Background="#F0F2F5" Padding="12">
            <StackPanel x:Name="MessagesPanel"/>
        </ScrollViewer>

        <Border Grid.Row="2" Background="White" BorderBrush="#E4E6EB" BorderThickness="0,1,0,0" Padding="12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox x:Name="MessageBox" Padding="12,10" VerticalContentAlignment="Center"
                         KeyDown="MessageBox_KeyDown" BorderBrush="#CCD0D5"/>
                <Button Grid.Column="1" Content="Send" Margin="8,0,0,0" Padding="20,10" Click="Send_Click"/>
            </Grid>
        </Border>
    </Grid>
</Window>
